import SwiftUI


extension EdgeInsets {
    
    static let listRow = EdgeInsets(top: 8, leading: 16 + UIDevice.current.deviceSpecificPadding, bottom: 8, trailing: 16 + UIDevice.current.deviceSpecificPadding)
    
}
